{
    "version": 2,
    "installCommand": "npm install",
    "buildCommand": "npx prisma generate && tsc -b",
    "outputDirectory": "dist",
    "routes": [
        {
            "src": "/.*",
            "dest": "dist/index.js"
        }
    ]
}